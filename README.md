## 1. 🧠 Установка Ollama (локальная LLaMA)

1. Скачайте установщик Ollama для своей ОС:
   👉 [https://ollama.com/download](https://ollama.com/download)

2. Скачайте модель LLaMA:

```bash
ollama pull llama3.2:3b
```

3. Проверьте, что модель работает:

```bash
ollama run llama3.2:3b
```

Если модель отвечает — всё установлено корректно.

---

## 2. 🗂️ Установка проекта

1. Склонировать проект себе:

```bash
git clone <URL вашего репозитория>
cd <название_папки_проекта>
```

2. Создать виртуальное окружение:

```bash
python -m venv .venv
```

3. Активировать окружение:

### Windows PowerShell:

```bash
.\.venv\Scripts\Activate.ps1
```

### Windows CMD:

```cmd
.\.venv\Scripts\activate.bat
```

### Linux / macOS:

```bash
source .venv/bin/activate
```

4. Установить зависимости:

```bash
pip install -r requirements.txt
```

---

## 3. 🚀 Запуск проекта

### 1️⃣ В первом терминале — запустить LLaMA:

```bash
ollama run llama3.2:3b
```

(это поднимает локальный сервер модели на `http://localhost:11434`)

### 2️⃣ Во втором терминале — активировать окружение и запустить бота:

```bash
.\.venv\Scripts\Activate.ps1
python main.py
```

Бот должен запуститься и начать слушать обновления Telegram.


